use NXCL::Package;
use NXCL::JSON;
use NXCL::YDump;
use NXCL::Environment;

my $env = NXCL::Environment->new;

my ($struct) = $env->eval_string(
  $ARGV[0]//die "No argument given"
);

print ydump(nxcl2json($struct));
