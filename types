repr nil
repr chars
repr cons
repr native
repr ref
repr bool

repr int

type string repr chars
type name repr chars

type bool repr int

type int repr int

type rat repr cons(int, int)

type result repr cons(bool, item?)

type list repr cons(item, list)?

type call repr cons(item, list)

type key repr chars

type pair repr cons(key, item)

type dict repr cons(pair, dict)?

type fexpr repr cons(scope, call)

type native repr native

repr bytes
type bytes repr bytes

type opaque repr cons(item, fexpr?)
